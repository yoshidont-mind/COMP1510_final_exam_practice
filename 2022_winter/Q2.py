# Let's execute the code from the second image and observe the output to answer the question.
set1 = set(['a', 'b', 'c', 'd', 'e'])
set2 = set(['d', 'e', 'f'])
set3 = set2.symmetric_difference(set1)

print(set3)
